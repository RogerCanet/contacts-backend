# Contacts API

A simple RESTful API to manage contacts, built with **Spring Boot 3**, **Java 25**, and **H2 in-memory database**.  
This project is meant to demonstrate a backend CRUD application, with validations, global exception handling, and Swagger documentation.

---

## 🚀 Features

- Create, read, update, delete contacts (CRUD)
- Search contacts by name, last name, email, or phone
- Input validation with meaningful error messages
- Global exception handling
- In-memory H2 database (resets on server restart)
- Swagger UI documentation and interactive API testing

---

## 📦 Technology Stack

- Java 25
- Spring Boot 3.5.7
- Maven
- H2 in-memory database
- Swagger / OpenAPI (springdoc)
- Optional: Postman for API testing

---